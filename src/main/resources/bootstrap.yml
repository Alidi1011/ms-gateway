spring:
  cloud:
    config:
      name: ms-gateway
      discovery:
        enabled: true
        service-id: ms-config-server
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8080
